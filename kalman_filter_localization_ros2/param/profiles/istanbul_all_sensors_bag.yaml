ekf_localization:
    ros__parameters:
      reference_frame_id: "map"
      robot_frame_id: "base_link"
      gnss_pose_topic: "/gnss_pose"
      imu_topic: "/sensing/imu/imu_data"
      odom_topic: "/odom"
      pub_period: 10

      # Istanbul all-sensors bags appear to publish gravity-compensated acceleration.
      # Set gravity_mps2 to 0.0 to avoid double subtracting gravity.
      gravity_mps2: 0.0

      # Istanbul IMU topic also provides a high-quality orientation estimate.
      # Fuse it to stabilize roll/pitch/yaw.
      use_imu_orientation: true
      use_imu_orientation_covariance: true

      # Tuned starting point (see tools/param_grid_istanbul_quick.json).
      # These values worked well across multiple Istanbul bags when evaluated
      # against INS ground truth.
      var_imu_w: 0.005
      var_imu_acc: 0.01
      max_imu_dt_sec: 1.0
      var_gnss_xy: 0.05
      var_gnss_z: 0.1
      var_odom_xyz: 0.2

      use_gnss: true
      use_odom: false
